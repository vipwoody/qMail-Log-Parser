/**
 * Stores info on each Clam AV update process
 * @author Rocks
 * v1.00
 */
public class clamAVUpdate
{
	private String timeStamp;
	private String daily_cdl_version;
	private String main_cvd_version;
	private String bytecode_cvd_version;
	
	/**
	 * initializes the Clam AV update info
	 * @param ts timestamp
	 * @param daily daily.cld version
	 * @param main main.cvd version
	 * @param bytecode bytecode.cvd version
	 */
	public clamAVUpdate(String ts, String daily, String main, String bytecode){}
	/**
	 * gets attributes
	 * @return var of accessor
	*/
	public var accessorFunctions(){}
	/**
	 * sets attributes
	 * @param n the variable to be set
	*/
	public void mutatorFunctions(var n){}
}